import React from 'react'

const ErrorMessage = ({error}) => {
  return (
    <div>
      perror occured due to wrong city name 
               refresh  {error}
    </div>
  )
}

export default ErrorMessage
